version: '1'

services:
  api:
    environment:
      - NODE_ENV=development
    build:
      context: .
      dockerfile: Dockerfile.dev
    volumes:
      - ./:/app
    container_name: ospiha-container
    expose:
      - '3000'
    ports:
      - '3000:3000'
    command: npm run dev